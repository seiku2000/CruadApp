{
  "name": "crudapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "server": "json-server --watch server/db.json --port 4000"
  },
  "devDependencies": {
    "json-server": "0.17.4",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}
